import React from 'react'

const AdminArea = (props) => {
  return (
    <div>
        {/* <button onClick={props.logoutHandler} style={{backgroundColor:'red', color:'white'}}>Logout</button> */}
        AdminArea
        
        </div>
  )
}

export default AdminArea